# Base URL of the Courier MVP API. Copy this file to .env and adjust if the API
# is not on the default port. Only VITE_-prefixed vars are exposed to the client.
VITE_API_BASE_URL=http://localhost:5080
